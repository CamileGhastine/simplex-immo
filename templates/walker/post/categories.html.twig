<a href="{{ path('index') }}">
    <span class="badge bg-secondary">Tous les articles</span>
</a>
{% for category in categories %}
    <a href="{{ path('index_by_category', {'id' : category.id}) }}">
        <span class="badge bg-secondary">{{ category.title }}</span>
    </a>
{% endfor %}